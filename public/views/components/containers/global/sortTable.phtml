<?php

$data = json_decode($_POST['data'], true);
$types = [['text' => "ASCENDING", 'value' => "asc"], ['text' => "DESCENDING", 'value' => "desc"]];
?>


<div class="just-a-list">
    <?= CreateComboBox("sort_type", "Sort Type", $types, false, $types[0]) ?>

    <?php foreach ($data as $value): ?>
        <li class="item"><?=  $value ?></li>
    <?php endforeach;?>
</div>